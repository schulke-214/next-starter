import { Theme } from 'styled-components';

const theme: Theme = {
	colors: {
		primary: '#000',
		secondary: '#fff'
	},
	spacings: {},
	typography: {
		primary: ['helvetica', 'sans-serif'],
		secondary: ['monospace']
	}
};

export default theme;
